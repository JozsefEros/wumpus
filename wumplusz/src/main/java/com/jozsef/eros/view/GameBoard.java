package com.jozsef.eros.view;

import com.jozsef.eros.GameBoardGenerator;


import java.util.Arrays;
/*
public class GameBoard extends GameBoardGenerator {

    public GameBoard(int rows, int cols) {
        super(rows, cols);
    }

    public static void main(String[] args) {
        // Example usage
        GameBoard gameBoard = new GameBoard(5, 5);
        gameBoard.fillGameBoard();
        gameBoard.printRoom();
    }

    @Override
    public char[][] getGameBoard() {
        char[][] board = new char[room.length][];
        for (int i = 0; i < room.length; i++) {
            board[i] = Arrays.copyOf(room[i], room[i].length);
        }
        return board;
    }


}
        */


